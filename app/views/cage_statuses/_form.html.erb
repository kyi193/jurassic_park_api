<%= form_with(model: cage_status) do |form| %>
  <% if cage_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cage_status.errors.count, "error") %> prohibited this cage_status from being saved:</h2>

      <ul>
        <% cage_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
