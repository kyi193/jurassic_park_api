<%= form_with(model: dinosaur_breed) do |form| %>
  <% if dinosaur_breed.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dinosaur_breed.errors.count, "error") %> prohibited this dinosaur_breed from being saved:</h2>

      <ul>
        <% dinosaur_breed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
